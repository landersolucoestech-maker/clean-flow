name: Shared visual refactor archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Shared visual refactor completed; this workflow is archived and read-only."
