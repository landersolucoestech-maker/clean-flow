name: Provider routing cleanup archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Provider routing cleanup completed; workflow archived and read-only."
