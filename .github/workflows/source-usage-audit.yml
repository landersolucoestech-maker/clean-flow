name: Source usage audit archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Source usage audit completed; this workflow is archived and read-only."
