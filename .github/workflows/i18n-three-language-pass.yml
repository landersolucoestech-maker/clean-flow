name: Three Language I18n Pass

on:
  push:
    branches: [dev]
    paths:
      - ".github/i18n-three-language.trigger"

permissions:
  contents: write

jobs:
  i18n:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply i18n pass
        run: |
          python scripts/i18n_three_language_pass.py
          python scripts/i18n_fill_parity.py
      - name: Install dependencies
        run: npm ci
      - name: Validate translation key parity
        run: |
          python - <<'PY'
          from pathlib import Path
          import re, sys
          base = Path('apps/web/src/app/i18n')
          sets = {}
          for lang in ('en','pt','es'):
              text = (base/f'{lang}.ts').read_text()
              sets[lang] = set(re.findall(r'^\s*"([^"]+)"\s*:', text, flags=re.M))
          all_keys = sets['en'] | sets['pt'] | sets['es']
          for lang in ('en','pt','es'):
              missing = sorted(all_keys - sets[lang])
              if missing:
                  print(lang, 'missing', len(missing), 'keys')
                  print('\n'.join(missing))
                  sys.exit(1)
          print('PASS:', len(all_keys), 'keys present in en/pt/es')
          PY
      - name: Lint
        run: npx eslint apps/web/src packages --max-warnings=0
      - name: Typecheck
        run: npm run typecheck
      - name: Tests
        run: npx vitest run apps/web/src
      - name: Build
        run: npm run build
      - name: Commit validated i18n foundation
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web/src i18n-visible-literals.txt
          git commit -m "refactor(i18n): enforce English Portuguese Spanish foundation" || true
          git push origin HEAD:dev
