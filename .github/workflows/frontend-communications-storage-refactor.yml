name: Frontend Communications Storage Refactor

on:
  push:
    branches: [dev]
    paths:
      - ".github/workflows/frontend-communications-storage-refactor.yml"

permissions:
  contents: write

jobs:
  refactor:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 1
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: node scripts/refactor-communications-storage.mjs
      - run: node scripts/frontend-audit.mjs
      - run: npx eslint apps/web/src packages --max-warnings=0
      - run: npm run typecheck
      - run: npx vitest run apps/web/src
      - run: npm run build
      - name: Commit communications storage refactor
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web/src/modules/communications/CommunicationsPage.tsx apps/web/src/modules/communications/services/messageAttachmentService.ts
          git diff --cached --quiet && exit 0
          git commit -m "refactor(frontend): isolate communications attachment storage"
          git push origin HEAD:dev
