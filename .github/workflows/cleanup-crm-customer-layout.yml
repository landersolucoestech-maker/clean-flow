name: Cleanup CRM Customer Layout

on:
  push:
    branches: [dev]
    paths:
      - ".github/scripts/cleanup_crm_customer_layout.py"
      - ".github/workflows/cleanup-crm-customer-layout.yml"

permissions:
  contents: write

jobs:
  cleanup-and-validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: dev
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply CRM layout cleanup
        run: python3 .github/scripts/cleanup_crm_customer_layout.py
      - name: Install dependencies
        run: npm ci
      - name: Lint
        run: npm run lint -- --max-warnings=0
      - name: Typecheck
        run: npm run typecheck
      - name: Tests
        run: npx vitest run apps/web/src
      - name: Build
        run: npm run build
      - name: Commit validated cleanup
        run: |
          rm .github/scripts/cleanup_crm_customer_layout.py
          rm .github/workflows/cleanup-crm-customer-layout.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add apps/web/src/modules/crm/customers/pages/CustomersPageContent.tsx \
                  .github/scripts/cleanup_crm_customer_layout.py \
                  .github/workflows/cleanup-crm-customer-layout.yml
          git commit -m "refine CRM layout to reference composition"
          git pull --rebase origin dev
          git push origin HEAD:dev
