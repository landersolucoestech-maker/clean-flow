# API boundary

Reserved application boundary for backend runtime that belongs to CleanFlow outside the existing Supabase project. Do not duplicate Supabase Edge Functions here without an explicit migration decision.

Expected source layout: `src/app`, `src/modules`, `src/integrations`, `src/shared`.
