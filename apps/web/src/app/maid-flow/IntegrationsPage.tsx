import { useState } from "react";
import { ChevronRight, CircleAlert, CircleCheck, Plug, X } from "lucide-react";
import type { IntegrationDefinition } from "../../../../../packages/domain/integration";
import { integrationFixtures } from "../../../../../packages/test-fixtures/integrations";

function statusCopy(status:IntegrationDefinition["status"]){return status==="connected"?"Connected":status==="attention_required"?"Attention required":"Not connected"}

export function IntegrationsPage(){const[selected,setSelected]=useState<IntegrationDefinition|null>(null);return <main className="min-w-0 flex-1 px-4 py-5 sm:px-6 lg:px-8 lg:py-7"><div className="mx-auto w-full max-w-[1150px]"><div className="mb-6 flex items-center gap-1.5 text-[11px] text-muted-foreground"><span>Maid Flow</span><ChevronRight className="h-3 w-3"/><span>Settings</span><ChevronRight className="h-3 w-3"/><span className="text-foreground/80">Integrations</span></div><header className="border-b border-border pb-5"><p className="mb-1.5 text-[10px] font-semibold uppercase tracking-[0.13em] text-primary">Settings</p><h1 className="text-[26px] font-semibold tracking-[-0.035em] sm:text-[29px]">Integrations</h1><p className="mt-1.5 max-w-2xl text-[13px] leading-5 text-muted-foreground">Manage external services used across scheduling, communications and accounting workflows.</p></header><section className="divide-y divide-border pt-3">{integrationFixtures.map((integration)=><div key={integration.id} className="flex flex-col gap-4 py-4 sm:flex-row sm:items-center"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-md border border-border bg-card text-muted-foreground"><Plug className="h-4 w-4"/></div><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h2 className="text-sm font-semibold">{integration.name}</h2><span className="inline-flex items-center gap-1 text-[10px] font-semibold text-muted-foreground">{integration.status==="connected"?<CircleCheck className="h-3 w-3 text-emerald-600"/>:integration.status==="attention_required"?<CircleAlert className="h-3 w-3 text-amber-600"/>:null}{statusCopy(integration.status)}</span></div><p className="mt-1 text-xs leading-5 text-muted-foreground">{integration.description}</p><div className="mt-2 flex flex-wrap gap-1.5">{integration.capabilities.map((capability)=><span key={capability} className="rounded border border-border bg-background px-2 py-0.5 text-[10px] text-muted-foreground">{capability}</span>)}</div></div><button type="button" onClick={()=>setSelected(integration)} className="h-8 rounded-md border border-border bg-card px-3 text-[11px] font-semibold hover:bg-muted">View details</button></div>)}</section></div>{selected&&<div className="fixed inset-0 z-50 flex justify-end" role="dialog" aria-modal="true" aria-labelledby="integration-details-title"><button type="button" className="absolute inset-0 bg-slate-950/30" onClick={()=>setSelected(null)} aria-label="Close integration details"/><div className="relative h-full w-full max-w-[480px] border-l border-border bg-background shadow-xl"><div className="flex h-16 items-center justify-between border-b border-border px-5"><div><h2 id="integration-details-title" className="text-sm font-semibold">{selected.name}</h2><p className="mt-0.5 text-[11px] text-muted-foreground">Integration details</p></div><button type="button" onClick={()=>setSelected(null)} className="grid h-8 w-8 place-items-center rounded-md hover:bg-muted"><X className="h-4 w-4"/></button></div><div className="space-y-5 p-5"><div className="rounded-md border border-border bg-card p-4"><p className="text-[10px] font-semibold uppercase tracking-wide text-muted-foreground">Connection state</p><p className="mt-2 text-sm font-semibold">{statusCopy(selected.status)}</p></div><div><h3 className="text-xs font-semibold">Capabilities</h3><ul className="mt-2 space-y-2">{selected.capabilities.map((capability)=><li key={capability} className="flex items-center gap-2 text-xs text-muted-foreground"><span className="h-1.5 w-1.5 rounded-full bg-muted-foreground/50"/>{capability}</li>)}</ul></div><div className="border-l-2 border-border bg-muted/40 px-3 py-2.5 text-xs leading-5 text-muted-foreground">Connection controls will appear here when this provider is available for configuration.</div></div></div></div>}</main>}
