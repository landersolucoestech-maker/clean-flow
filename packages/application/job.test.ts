import{describe,expect,it}from"vitest";import{createJob}from"./job";import{MockJobRepository}from"../data/mock-job-repository";import{serviceFixtures}from"../test-fixtures/services";
describe("job application",()=>{it("rejects a frequency not allowed by the selected service",async()=>{const repository=new MockJobRepository();const service=serviceFixtures.find((row)=>row.id==="service-one-time")!;await expect(createJob(repository,{customerId:"customer-1",serviceId:service.id,locationId:"location-1",startsAt:new Date().toISOString(),durationMinutes:120,price:{amountMinor:10000,currency:"USD"},assignedStaffIds:[],recurrence:{type:"none"},serviceFrequency:"weekly"},service)).rejects.toThrow("Frequency is not allowed")});it("uses stable assignment IDs",async()=>{const repository=new MockJobRepository();const service=serviceFixtures[0];const row=await createJob(repository,{customerId:"customer-1",serviceId:service.id,locationId:"location-1",startsAt:new Date().toISOString(),durationMinutes:120,price:{amountMinor:10000,currency:"USD"},assignedTeamId:"team-1",assignedStaffIds:["staff-2"],recurrence:{type:"none"},serviceFrequency:"weekly"},service);expect(row.assignedTeamId).toBe("team-1");expect(row.assignedStaffIds).toEqual(["staff-2"])});});
